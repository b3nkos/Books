fun main() {
    var count = 1

    // Loop that runs while count <= 5
    while(count <= 5) {
        println("Counting... $count")
        count += 1
    }

    // Bonus: countdown version
    var reverse = 5;
    while (reverse >= 1) {
        println("Countdown ... $reverse");
        reverse -= 1
    }
}